{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make directory",
            "type": "shell",
            "command": "${workspaceFolder}\\.scripts\\make_dir.bat ${input:problemNo}",
            "problemMatcher": []
        }
    ],
    "inputs": [
        {
            "id": "problemNo",
            "description": "enter the problem number",
            "type": "promptString"
        }
    ]
}
